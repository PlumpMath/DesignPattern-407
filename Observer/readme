观察者模式
定义：观察者模式定义了对象之间的一对多依赖，这样一来，当一个对象改变状态时，它的所有依赖者都会收到通知并且更新。

设计原则：
1.为了交互对象之间的松耦合设计而努力，松耦合的设计能够将对象之间的互相依赖降到最低，建立起有弹性，能够应对变化的面向对象系统。

松耦合的威力：当两个对象之间松耦合，他们依然可以交互，但是不太清楚彼此的细节。
观察者模式提供了一种对象设计，让主题和观察者之间松耦合。只要他们的仍然遵守接口，我们就可以在自由的改变他们。